# TensorRT-LLM - NVIDIA optimized inference engine with NVFP4 support
# Already installed in tensorrt-llm base image

# MLflow - For querying MLflow Model Registry and fetching model metadata
mlflow~=3.6.0

# Requests - For HTTP requests (Keycloak authentication)
requests>=2.28.0

# OpenAI client - For communicating with TensorRT-LLM's OpenAI-compatible API
openai>=1.0.0

# Gradio - Web UI with theming support
gradio>=4.42.0

# OpenAI Harmony - Official parser for gpt-oss harmony format responses
openai-harmony>=0.1.0
