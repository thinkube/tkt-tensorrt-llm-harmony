apiVersion: thinkube.io/v1
kind: ThinkubeDeployment

spec:
  deployment:
    type: component
    name: tensorrt
    replicas: 0

  containers:
    - name: inference
      build: .
      port: 7860
      size: xlarge
      gpu:
        count: 1
        memory: "80Gi"
      health: /health
      test:
        enabled: false

  routes:
    - path: /
      to: inference
